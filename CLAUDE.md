1. Tổng case đúng là 232 (45+43+48+32+24+21+19, xác nhận bằng ID duy nhất, không trùng) — README vốn đã đúng, chỉ Section 8 cần sửa lại từ "231 Pass" thành census đầy đủ. 227 của automation JSON là con số khác hẳn (subset tự động hoá được), không phải tổng test-plan.
2. TC-UC02-4: đúng là code tạo ra WALK thứ hai tự động (đây là hành vi ĐÚNG đã fix) — Section 8 trước đó mô tả sai thành "không restart" nhưng vẫn đánh Pass, đã sửa lại mô tả cho khớp.
3. Drain cap 60s: chỉ là cap phần gia hạn, tổng pha thực tế lên tới 90s (30+60) — CC-TIME-02 từng ghi sai là tổng pha cap ở 60s, đã sửa; 01's TC-UC05-1/3 vốn đã đúng.
4. Railway timing: thời gian thật TRAIN_APPROACHING→CLOSED là ~8s (5s warning + 3s gate motion), không phải 44-46s — con số đó là budget thiết kế RC-03 (bao gồm 25s clearance không liên quan), đã tách rõ 2 con số.
5. Phương pháp đo: central_log.txt không ghi heartbeat/phase transition của Lx/RLx (chỉ Central, chỉ log khi có transition) — viết lại toàn bộ Table 11 + PA-TIME-01/06 cho đúng cái gì thực sự đo được.
6. Watchdog kill -STOP: phát hiện thêm bug — TC-FAULT-12/13 nhầm trích dẫn TC-FAULT-16 (case phản chứng) thay vì TC-FAULT-17 (case debugger thật), đã sửa; mâu thuẫn 02 vs 05 vẫn để ngỏ đúng như yêu cầu (cần máy QNX thật).
7. 07-cross-node: thêm topology 10-VM thật (từ QNX_DEPLOYMENT_RUN_GUIDE.md) làm chuẩn chính, giữ bản 3-VM cũ làm fallback rút gọn có ghi chú rõ; quét sạch nốt vài chỗ SIGNAL -> còn sót trong JSON.
8. RC-11: gỡ claim sai rằng có diagnostic timeout demo được qua x/r — thực tế cơ chế đó đã bị xoá (dead code), x/r mô phỏng lỗi/sửa gate chứ không phải sensor.